import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "The First Year Map | Skifte Collective",
};

export default function FirstYearLayout({ children }: { children: React.ReactNode }) {
  return children;
}
