import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Browse Swaps | Skifte Collective",
};

export default function BrowseLayout({ children }: { children: React.ReactNode }) {
  return children;
}
